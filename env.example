# Email Configuration
# Choose one of the following email service configurations:

# Option 1: Gmail SMTP (Recommended for testing)
EMAIL_SERVICE=gmail
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password

# Option 2: Microsoft 365/Exchange Online (Recommended for business)
EMAIL_SERVICE=microsoft365
EMAIL_USER=your-email@yourdomain.com
EMAIL_PASSWORD=your-password-or-app-password

# Option 3: Custom SMTP (for production services like SendGrid, Mailgun, etc.)
# EMAIL_SERVICE=custom
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_SECURE=false
# EMAIL_USER=your-smtp-username
# EMAIL_PASSWORD=your-smtp-password

# Server Configuration
PORT=3000
NODE_ENV=development

# Security
# Add your domain to CORS origins in server.js if needed 